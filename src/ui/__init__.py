"""Presentation layer for the EcoLearn Streamlit app.

This package holds the *visual* design system — the theme (single source of
truth for colours, typography, spacing) and reusable themed components. It
imports nothing from the platform internals; pages get their data from
`src.platform_api` and their looks from here.
"""
